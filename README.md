# Emotion Classifier with HuggingFace 🤗

Fine-tuning `distilbert-base-uncased` on the [`dair-ai/emotion`](https://huggingface.co/datasets/dair-ai/emotion) dataset.

## Setup

```bash
pip install -r requirements.txt
